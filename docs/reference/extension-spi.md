---
title: Extension SPI
description: The named SPI facades and the symbols extension authors implement against.
---

<!-- Generated by scripts/gen_spi_docs.py. Do not edit by hand: regenerate with
     `uv run python scripts/gen_spi_docs.py` (drift-gated by tests/docs_drift/test_extension_spi_generated.py). -->

# Extension SPI

Extension points — transports, stores, envelope mappers, projection locks, codecs, lifecycle
hooks — are carried by the subpackage facades below. The listed import path is the contract:
SPI symbols change on a shorter announced cycle than the public API (changelog plus one release),
and anything under an `_internal/` path is unversioned framework machinery with no stability
guarantee.

## `waku.messaging.transport`

Broker transports and the wire seam (mappers, metadata, dispositions).

`WIRE_CONTENT_TYPE` ·
`ConsumeCallback` ·
`ConsumeDisposition` ·
`EnvelopeMetadata` ·
`IEnvelopeMapper` ·
`IListener` ·
`ISender` ·
`ITransport` ·
`MalformedMetadataError` ·
`Subscription` ·
`TransportFactory` ·
`UnsupportedContentTypeError` ·
`metadata_from_headers` ·
`wire_headers_of`

## `waku.messaging.transport.faststream`

FastStream-based broker adapters and per-broker mapper contracts.

`DefaultKafkaEnvelopeMapper` ·
`DefaultRabbitEnvelopeMapper` ·
`FastStreamKafkaTransport` ·
`FastStreamRabbitTransport` ·
`FastStreamTransportBase` ·
`IKafkaEnvelopeMapper` ·
`IRabbitEnvelopeMapper` ·
`KafkaOutgoing` ·
`RabbitOutgoing` ·
`kafka_transport` ·
`rabbit_transport`

## `waku.messaging.endpoints`

The endpoint model and executor animated by routing.

`DEFAULT_ENDPOINT_URI` ·
`BrokerEndpointEntry` ·
`Endpoint` ·
`EndpointEntry` ·
`EndpointExecutor` ·
`EndpointExecutorFactory` ·
`ExecutionOutcome` ·
`ExecutionResult` ·
`LocalQueueEntry`

## `waku.messaging.durability`

Messaging store-port contracts and the durability store composite.

`DefaultDurabilityStore` ·
`IDeadLetterStore` ·
`IDurabilityStore` ·
`IInboxStore` ·
`IOutboxStore`

## `waku.messaging.sequence`

Per-group durable sequence allocation contracts.

`GroupId` ·
`ISequenceAllocator` ·
`allocate_sequence_by_id`

## `waku.messaging.outbox`

Outbox models and the relay.

`OutboxMessage` ·
`OutboxRelay` ·
`OutboxRelayConfig` ·
`OutboxStatus`

## `waku.messaging.inbox`

Durable inbox models.

`EndpointUri` ·
`HandlerDestination` ·
`InboxEntry` ·
`InboxStatus` ·
`handler_destination`

## `waku.messaging.errors`

Error policies, dead-letter models, replay, and the DLQ worker.

`DeadLetterDestinationKind` ·
`DeadLetterEntry` ·
`DeadLetterQuery` ·
`DeadLetterStatus` ·
`DuplicateErrorPolicyError` ·
`ErrorPolicy` ·
`ErrorPolicyEvaluator` ·
`ErrorPolicyRegistry` ·
`FailureContext` ·
`PolicyOutcome` ·
`ReplayExecutor` ·
`RetryAction` ·
`RetryStage` ·
`validate_requested_lease`

## `waku.messaging.sending`

Sending-failure policies applied by the outbox relay.

`SendingFailureContext` ·
`SendingFailureEvaluator` ·
`SendingFailurePolicy` ·
`SendingFailurePolicyRegistry`

## `waku.eventsourcing.store`

Event store contracts (with snapshot/checkpoint facets); in-memory reference store.

`ICheckpointStore` ·
`IEventReader` ·
`IEventStore` ·
`IEventWriter` ·
`ISnapshotStore` ·
`IdempotencyVerdict` ·
`InMemoryEventStore` ·
`check_expected_version` ·
`classify_idempotency` ·
`enrich_metadata`

## `waku.eventsourcing.snapshot`

Snapshot model, strategy, and migration contracts.

`EventCountStrategy` ·
`ISnapshotMigration` ·
`ISnapshotStrategy` ·
`InMemorySnapshotStore` ·
`Snapshot` ·
`SnapshotConfig` ·
`SnapshotConfigRegistry` ·
`SnapshotMigrationChain`

## `waku.eventsourcing.projection`

Projection contracts, polling configuration, and lease configuration.

`Checkpoint` ·
`InMemoryCheckpointStore` ·
`LeaseConfig` ·
`PollingConfig`

## `waku.eventsourcing.serialization`

Event and snapshot-state serializer contracts.

`EventTypeRegistry` ·
`IEventSerializer` ·
`ISnapshotStateSerializer` ·
`JsonEventSerializer` ·
`JsonSnapshotStateSerializer` ·
`es_default_retort`

## `waku.serialization`

Payload codec and the upcasting toolkit.

`FnUpcaster` ·
`IPayloadUpcaster` ·
`PayloadCodec` ·
`UpcasterChain` ·
`UpcasterChainError` ·
`add_field` ·
`noop` ·
`remove_field` ·
`rename_field` ·
`upcast`

## `waku.backends.sqlalchemy`

SQLAlchemy durability backend: entry point, store adapters, tables.

`CheckpointTables` ·
`DeadLetterTables` ·
`EnumFromValues` ·
`EventStoreTables` ·
`InboxTables` ·
`LeaseTables` ·
`OutboxTables` ·
`PostgresAdvisoryLease` ·
`PostgresLease` ·
`SequenceTables` ·
`SnapshotTables` ·
`SqlAlchemyBackend` ·
`SqlAlchemyCheckpointStore` ·
`SqlAlchemyDeadLetterStore` ·
`SqlAlchemyEventStore` ·
`SqlAlchemyInboxStore` ·
`SqlAlchemyOutboxStore` ·
`SqlAlchemySequenceAllocator` ·
`SqlAlchemySnapshotStore` ·
`SqlAlchemyUnitOfWork` ·
`bind_checkpoint_tables` ·
`bind_dead_letter_tables` ·
`bind_event_store_tables` ·
`bind_inbox_tables` ·
`bind_lease_tables` ·
`bind_outbox_tables` ·
`bind_sequence_tables` ·
`bind_snapshot_tables` ·
`make_sqlalchemy_event_store`

## `waku.backends.memory`

In-memory durability backend (whole-app wiring stub).

`MemoryBackend`

## `waku.backends.testing`

Backend-author conformance kit: assembly and facet contract suites.

`BackendAssemblyContract` ·
`CheckpointStoreContract` ·
`DeadLetterStoreContract` ·
`EventStoreContract` ·
`EventStoreFactory` ·
`InboxStoreContract` ·
`ItemAdded` ·
`OrderCreated` ·
`OrderShipped` ·
`OutboxStoreContract` ·
`SequenceAllocatorContract` ·
`SnapshotStoreContract` ·
`make_envelope` ·
`make_outbox_message`

## `waku.extensions`

Module and application lifecycle hooks.

`DEFAULT_EXTENSIONS` ·
`AfterApplicationInit` ·
`ApplicationExtension` ·
`ExtensionRegistry` ·
`ModuleExtension` ·
`OnApplicationInit` ·
`OnApplicationShutdown` ·
`OnContainerBuilt` ·
`OnModuleConfigure` ·
`OnModuleDestroy` ·
`OnModuleInit` ·
`OnModuleRegistration` ·
`RegistryAggregator`
