# CHANGELOG

{%    if ctx.changelog_mode == "update"
%}{#    # Modified insertion flag to insert a changelog header directly
        # which conveniently puts the insertion flag incognito when reading raw RST
#}{{
        insertion_flag ~ "\n"

}}{%  endif
%}
